"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.cssInterop = exports.createElementAndCheckCssInterop = exports.renderWithInterop = exports.render = exports.interopComponents = void 0;
const react_1 = require("react");
const globals_1 = require("./globals");
const prop_mapping_1 = require("./native/prop-mapping");
exports.interopComponents = new Map();
function render(jsx, type, props, ...args) {
    if (process.env.NODE_ENV === "development") {
        if (type === "react-native-css-interop-jsx-pragma-check") {
            return true;
        }
    }
    const interop = exports.interopComponents.get(type);
    if (!interop || !interop.check(props))
        return jsx(type, props, ...args);
    return jsx(interop.type, props, ...args);
}
exports.render = render;
function renderWithInterop(jsx, interop, ...args) {
    return interop(jsx, ...args);
}
exports.renderWithInterop = renderWithInterop;
function createElementAndCheckCssInterop(...args) {
    const type = args[0];
    if (!type)
        return (0, react_1.createElement)(...args);
    const interop = exports.interopComponents.get(type);
    return !interop || !interop.check(args[1])
        ? (0, react_1.createElement)(...args)
        : interop.createElementWithInterop(args[1], args.slice(2));
}
exports.createElementAndCheckCssInterop = createElementAndCheckCssInterop;
function cssInterop(component, mapping, interop) {
    const config = (0, prop_mapping_1.getNormalizeConfig)(mapping);
    interop ??= globals_1.defaultInteropRef.current;
    let render = ({ children, ___pressable, ...props }, ref) => {
        if (ref) {
            props.ref = ref;
        }
        if (___pressable) {
            return (0, react_1.createElement)(component, props, children);
        }
        else {
            return (0, react_1.createElement)(...interop(component, config, props, children));
        }
    };
    if (process.env.NODE_ENV === "development") {
        if (typeof component === "string") {
            render.displayName = `CSSInterop.${component}`;
        }
        else {
            render.displayName = `CSSInterop.${component.displayName ?? component.name ?? "unknown"}`;
        }
    }
    render = (0, react_1.forwardRef)(render);
    const checkArray = (props) => props.some((prop) => {
        return Array.isArray(prop) ? checkArray(prop) : globals_1.styleMetaMap.has(prop);
    });
    const interopComponent = {
        type: render,
        createElementWithInterop(props, children) {
            return (0, react_1.createElement)(...interop(component, config, props, children));
        },
        check(props) {
            for (const [targetProp, { sources, nativeStyleToProp },] of config.config) {
                if (nativeStyleToProp)
                    return true;
                for (const source of sources) {
                    if (typeof props[source] === "string") {
                        return true;
                    }
                }
                const target = props[targetProp];
                const targetMeta = globals_1.styleMetaMap.get(target);
                if (Array.isArray(target)) {
                    if (checkArray(target)) {
                        return true;
                    }
                }
                else if (targetMeta && !targetMeta.alreadyProcessed) {
                    return true;
                }
            }
            return false;
        },
    };
    exports.interopComponents.set(component, interopComponent);
}
exports.cssInterop = cssInterop;
//# sourceMappingURL=render.js.map