/**
 * @deprecated Import from `../theme` instead — that is where the design system
 * lives now (tokens, typography, and the light/dark semantic themes).
 *
 * Re-exported here only so existing `constants/themes` imports keep resolving.
 */

export { lightTheme, darkTheme, themes, default } from '../theme';
